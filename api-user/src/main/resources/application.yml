spring:
  application:
    name: api-user

  profiles:
    active: local

  threads:
    virtual:
      enabled: true

  config:
    import:
      - db-config.yml
      - logging.yml

server:
  port: 8080